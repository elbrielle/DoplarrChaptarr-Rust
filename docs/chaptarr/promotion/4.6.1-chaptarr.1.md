# Promotion record — DoplarrChaptarr 4.6.1-chaptarr.1 (DRAFT, awaiting sign-off)

**Status: staged.** Nothing is tagged, published, or announced by this
record. Beta graduation is claimed only by the sign-off block below, which
is deliberately empty.

## Versions

- DoplarrChaptarr: `4.6.1-chaptarr.1`, branch `sprint-3-native-hook`
  (release notes: `CHANGELOG.md` § 4.6.1-chaptarr.1).
- Chaptarr: `0.9.936.0`, image `chaptarr/chaptarr:0.9.936`, digest
  `sha256:e7aa84d7d65c460c914d36438529592181dca9c60146efdd4914a8baad2953be`.
- Upstream doplarr_rs: synced @ `9533084` (Sportarr carried).
- Candidate image: not yet built/tagged — produced at tag time per the
  checklist (`nix build .#dockerImage` + `.github/ci/smoke-image.sh`);
  `nix flake check` was not runnable on the staging machine (no nix) and
  belongs to the tag-time gate run.

## Mutation proof

Full disposable-instance canary, 2026-08-28, per
`docs/chaptarr/RELEASE_CHECKLIST.md`: all twelve mutation cases executed —
ten PASS, the 202-pending path observed three times and the 409 path once
(bare-error variant), the mid-settle injection recorded unreachable live
(synchronous imports) with its semantics unit-covered. Every checklist
verification bullet is answered, and both prove-live sections are marked.
Evidence, findings (nine, each fixed at the root or recorded), and the
post-merge smoke: `docs/chaptarr/canary/2026-08-28-0.9.936.md`. Automated
gates green at the branch tip: `cargo fmt --check`,
`cargo test --workspace` (125 lib + contract tests),
`cargo clippy --workspace --all-targets -- -D warnings` (default and
`canary` feature), release build excludes the canary driver.

## Rollback

No image or tag moves with this record. The previous production deployment
(the Clojure DoplarrChaptarr bot) is untouched and remains the rollback
target; the disposable canary instance was destroyed after the run and
verified gone.

## Remaining before beta graduation (Elisha-only)

- [ ] Read-only interaction proof (real Discord bot, real guild,
      abandon-without-Request cases, cover rendering) — needs her token and
      eyes. Result: ______________________
- [ ] Tag-time gates on a nix-capable machine: `nix flake check`,
      `nix build .#dockerImage`, `.github/ci/smoke-image.sh` — image
      digest + checksum recorded here: ______________________
- [ ] Promotion sign-off (name, date): ______________________
- [ ] `git push`, release tag, published artifact: ______________________
- [ ] Beta graduation statement: ______________________
